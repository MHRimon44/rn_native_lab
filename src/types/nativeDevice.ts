export type NativeDeviceSummary = {
  appVersion: string;
  buildNumber: string;
  deviceModel: string;
  osVersion: string;
  batteryLevel: number;
  isBatteryCharging: boolean;
  source: string;
};
